---
"blume": patch
---

Add a `transparentHeader` prop to `PageLayout`. The header starts with no background and its chrome in white, so it can sit over a dark hero at the top of a landing page, and returns to the frosted bar as soon as the page scrolls; the search dialog keeps the page's own colors throughout.
